@php
    use yii\helpers\Url;
    $view->registerJsFile("https://cdn.jsdelivr.net/npm/handlebars@latest/dist/handlebars.js",[
    'depends' => [
        \yii\web\JqueryAsset::className()
        ]
    ]);
@endphp
@yield('pre_page')
@if ($flash !== false)
    {!! $view->render("@backend/views/parts/FlashMessage.blade") !!}
@endif
@yield('content')
@yield('js_template')
@yield('pos_page')
